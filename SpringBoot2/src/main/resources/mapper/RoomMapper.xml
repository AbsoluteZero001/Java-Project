<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springboot.springboot2.mapper.RoomMapper">

    <!-- 查询指定楼层房间 -->
    <select id="roomsOfFloor" resultType="com.springboot.springboot2.pojo.Room">
        SELECT room_id,
               room_code,
               room_floor_id,
               built_up_area,
               room_status
        FROM room
        WHERE room_floor_id = #{floorId}
    </select>

    <!-- 根据用户房间ID查询房间列表 -->
    <select id="getRoomsByUserRoomId" resultType="com.springboot.springboot2.pojo.Room">
        SELECT room_id,
               room_code,
               room_floor_id,
               built_up_area,
               room_status
        FROM room
        WHERE room_id = #{userRoomId}
    </select>

</mapper>
